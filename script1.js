/* let age = prompt("how old are you?");

class Driver {
  constructor(age) {
    this.age = age;
    this.requiredAge = 18;
  }

  checkAge() {
    if (this.age >= this.requiredAge) {
      alert("You are old enough to drive");
    } else if (this.age < this.requiredAge) {
      alert("You are too young to drive");
    } else {
      alert("Something went wrong. Make sure to only enter numbers");
    }
  }
}

let myDriver = new Driver(age);

myDriver.checkAge();
 */
