/* let studentsNr = parseInt(prompt("How many students are there in the class?"));

let students = [];

for (i = 0; i < studentsNr; i++) {
  let name = prompt("What is the name of student #" + (i + 1));
  students.push(name);
}

document.body.innerHTML = students; */
