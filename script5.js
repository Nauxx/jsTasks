/* let array = [];

for (i = 0; i < 6; i++) {
  array.push(i + 1);
}

let doubledArray = array.map(item => {
  return item * 2;
});

let rand = Math.floor(Math.random() * doubledArray.length);

document.body.innerHTML = doubledArray + "</br>" + doubledArray[rand];

console.log(doubledArray); */
